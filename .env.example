# AI-Mixtapes Environment Configuration
# ⚠️ SECURITY WARNING ⚠️
# Never commit your actual .env file containing real tokens
# This is only an example file - copy to .env and add your tokens

# Spotify API Configuration
SPOTIFY_CLIENT_ID=your_client_id_here
SPOTIFY_CLIENT_SECRET=your_client_secret_here

# Keep your tokens secure:
# 1. Never commit .env to version control
# 2. Add .env to .gitignore
# 3. Use secure methods to share tokens with team members
# 4. Rotate tokens regularly
# 5. Use environment variables in CI/CD pipelines